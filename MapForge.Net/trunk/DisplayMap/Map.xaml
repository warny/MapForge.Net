<UserControl x:Class="DisplayMap.Map"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300"
             x:Name="displayMap" Width="458.333"
             Background="White">
    <DockPanel>
        <Canvas x:Name="TilesHolder">

        </Canvas>
        <Canvas Height="{Binding Path=ActualHeight, ElementName=displayMap}">
            <TextBox x:Name="GeoPosition" Height="23" Canvas.Left="10" Text="GeoPosition" Canvas.Top="10" Width="426"/>
        </Canvas>
    </DockPanel>
</UserControl>
